#!/usr/bin/env bash
set -euo pipefail

source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd -P)/_common.sh"

sync_deps

# Re-run tests automatically on file changes.
# Everything after `--` is passed to pytest.
exec uv run ptw -- -v -s --cache-clear "$@"
